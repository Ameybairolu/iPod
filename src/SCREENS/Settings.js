import classes from './GamesSettingsSongsArtists.module.css';
const Settings = () => {
    // Dummy for displaying some different screen when the active tab in menu is 'settings'
    return (
        <div className={classes.displaySettings}>
        </div>
    );
}

export default Settings;