import classes from './GamesSettingsSongsArtists.module.css';
const Songs = () => {
    // Dummy for displaying some different screen when the active tab in menu is 'songs'
    return (
        <div className={classes.displaySongs}>
        </div>
    );
}

export default Songs;