import classes from './GamesSettingsSongsArtists.module.css';
const Artists = () => {
    // Dummy for displaying some different screen when the active tab in menu is 'Artists'
    return (
        <div className={classes.displayArtists}>
        </div>
    );
}

export default Artists;