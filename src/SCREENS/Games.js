import classes from './GamesSettingsSongsArtists.module.css';
const Games = () => {
    // Dummy for displaying some different screen when the active tab in menu is 'Games'
    return (
        <div className={classes.displayGames}>
        </div>
    );
}

export default Games;